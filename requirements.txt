# Emma's Health & Gut Rhythm Web Service
# Standard library modules (sqlite3, http.server, json, urllib)
# Optional pre-compiled driver for Render PostgreSQL cloud persistence:
psycopg2-binary>=2.9.9

